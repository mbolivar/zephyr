# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/usb/nordic,nrf-usbd.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/usb/nordic,nrf-usbd.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Nordic nRF52 USB device controller
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: nordic,nrf-usbd
  interrupts: true
  maximum-speed:
    $ref: /schemas/types.yaml#/definitions/string
    description: Configures USB controllers to work up to a specific speed. Valid
      arguments are "super-speed", "high-speed", "full-speed" and "low-speed". If
      this is not passed via DT, USB controllers should use their maximum hardware
      capability.
    enum:
    - low-speed
    - full-speed
    - high-speed
    - super-speed
  num-bidir-endpoints:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: |-
      Number of bi-directional endpoints supported by hardware
      (including EP0)
  num-in-endpoints:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: |-
      Number of IN endpoints supported by hardware
      (including EP0 IN)
  num-isoin-endpoints:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: Number of ISOIN endpoints supported by hardware
  num-isoout-endpoints:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: Number of ISOOUT endpoints supported by hardware
  num-out-endpoints:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: |-
      Number of OUT endpoints supported by hardware
      (including EP0 OUT)
  reg: true
  vbus-gpios:
    $ref: /schemas/types.yaml#/definitions/phandle-array
    description: Control VBUS via GPIO pin.
required:
- compatible
- interrupts
- num-bidir-endpoints
- num-isoin-endpoints
- num-isoout-endpoints
- reg
additionalProperties: true
