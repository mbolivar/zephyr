# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/crypto/nordic,nrf-ccm.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/crypto/nordic,nrf-ccm.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Nordic nRF family CCM (AES CCM mode encryption)
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: nordic,nrf-ccm
  headermask-supported:
    type: boolean
    description: |-
      Indicates whether the CCM peripheral supports the headermask register to
      set the PDU header bitmask used to derive the additional authentication
      data (AAD) in CCM for Bluetooth Low Energy ACL, BIS and CIS PDUs.
  interrupts: true
  length-field-length-8-bits:
    type: boolean
    description: |-
      Indicates whether the CCM peripheral supports the extended length
      (8 bits) of the LENGTH field in encrypted/decrypted packets.
      If not set, only the default length (5 bits) is supported.
  reg: true
required:
- compatible
- interrupts
- reg
additionalProperties: true
