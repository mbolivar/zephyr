# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/rtc/zephyr,rtc-emul.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/rtc/zephyr,rtc-emul.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Emulated RTC device
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: zephyr,rtc-emul
  alarms-count:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: |-
      Number of alarms supported by RTC device. The number of
      alarms defaults to 0, which indicates that the RTC has
      no alarms.
    default: 0
required:
- compatible
additionalProperties: true
