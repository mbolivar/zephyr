# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/flash_controller/zephyr,sim-flash.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/flash_controller/zephyr,sim-flash.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Simulated flash memory
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: zephyr,sim-flash
  erase-value:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: Value of erased flash cell
    enum:
    - 0xff
    - 0x0
  memory-region:
    $ref: /schemas/types.yaml#/definitions/phandle
    description: |-
      Memory region used by the simulated flash memory. If this option is used
      the memory that is used by the simulated flash memory is not erased.
  no-explicit-erase:
    type: boolean
    description: |-
      When set, this instance behaves as a RAM-like device that does not
      require explicit erase before write. Overrides the global
      CONFIG_FLASH_SIMULATOR_EXPLICIT_ERASE Kconfig option for this instance,
      allowing different simulator instances to have different erase modes.
required:
- compatible
additionalProperties: true
