# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/p_state/zephyr,native-sim-pstate.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/p_state/zephyr,native-sim-pstate.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Native Sim mock implementation of custom properties for performance state (pstate)
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: zephyr,native-sim-pstate
  load-threshold:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: |-
      CPU load threshold, in percent, for entering this performance state. Once
      the CPU load exceeds this threshold, it becomes a valid performance state
      for the P-state driver to switch to.
  pstate-id:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: Identifier of performance state.
required:
- compatible
- pstate-id
additionalProperties: true
