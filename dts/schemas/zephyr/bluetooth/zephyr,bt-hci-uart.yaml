# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/bluetooth/zephyr,bt-hci-uart.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/bluetooth/zephyr,bt-hci-uart.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Bluetooth HCI behind a UART device (H:4)
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: zephyr,bt-hci-uart
  bt-hci-bus:
    $ref: /schemas/types.yaml#/definitions/string
    description: Bus of the transport
    enum:
    - virtual
    - usb
    - pccard
    - uart
    - rs232
    - pci
    - sdio
    - spi
    - i2c
    - smd
    - virtio
    - ipc
    default: uart
  bt-hci-name:
    $ref: /schemas/types.yaml#/definitions/string
    description: Name of the HCI transport
    default: H:4
  bt-hci-quirks:
    $ref: /schemas/types.yaml#/definitions/string-array
    description: HCI device quirks
    enum:
    - no-reset
    - no-auto-dle
  bt-hci-vs-ext:
    type: boolean
    description: Zephyr HCI vendor extensions are supported
  reset-assert-duration-ms:
    description: Minimum duration to hold the reset-gpios pin low for. If not specified
      no delay beyond the code path execution time is guaranteed.
  reset-gpios:
    $ref: /schemas/types.yaml#/definitions/phandle-array
required:
- compatible
additionalProperties: true
