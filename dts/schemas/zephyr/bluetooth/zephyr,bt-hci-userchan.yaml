# SPDX-License-Identifier: Apache-2.0
# Converted from dts/bindings/bluetooth/zephyr,bt-hci-userchan.yaml by migrate_binding.py.
%YAML 1.2
---
$id: http://devicetree.org/schemas/zephyr/bluetooth/zephyr,bt-hci-userchan.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Bluetooth HCI provided by a Linux HCI user channel socket
maintainers:
- Zephyr Devicetree subsystem <devel@lists.zephyrproject.org>
properties:
  compatible:
    const: zephyr,bt-hci-userchan
  bt-hci-bus:
    $ref: /schemas/types.yaml#/definitions/string
    description: Bus of the transport
    enum:
    - virtual
    - usb
    - pccard
    - uart
    - rs232
    - pci
    - sdio
    - spi
    - i2c
    - smd
    - virtio
    - ipc
    default: uart
  bt-hci-name:
    $ref: /schemas/types.yaml#/definitions/string
    description: Name of the HCI transport
    default: HCI User Channel
  bt-hci-quirks:
    $ref: /schemas/types.yaml#/definitions/string-array
    description: HCI device quirks
    enum:
    - no-reset
    - no-auto-dle
  bt-hci-vs-ext:
    type: boolean
    description: Zephyr HCI vendor extensions are supported
required:
- compatible
additionalProperties: true
