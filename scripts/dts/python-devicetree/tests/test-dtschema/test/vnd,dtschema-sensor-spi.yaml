# SPDX-License-Identifier: BSD-3-Clause
%YAML 1.2
---
$id: http://devicetree.org/schemas/test/vnd,dtschema-sensor-spi.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Test sensor on SPI (multi-bus pattern, spi variant)

maintainers:
  - python-devicetree tests

allOf:
  - $ref: vnd,dtschema-sensor-common.yaml#

properties:
  compatible:
    const: vnd,dtschema-sensor

  reg:
    maxItems: 1

  spi-extra:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: property that only exists on the SPI variant

required:
  - compatible

additionalProperties: true
